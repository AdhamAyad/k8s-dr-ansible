[defaults]
inventory = ./inventory
remote_user = Adham
host_key_checking = False
timeout = 30

[privilege_escalation]
become = True
become_method = sudo
become_ask_pass = False

[ssh_connection]
retries = 3
