apiVersion: v1
kind: PersistentVolume
metadata:
  name: etcd-backup-nfs-pv
spec:
  capacity:
    storage: 1Gi 
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain 
  nfs:
    server: 172.16.10.10 
    path: "/opt/k8s-backups/etcd"
